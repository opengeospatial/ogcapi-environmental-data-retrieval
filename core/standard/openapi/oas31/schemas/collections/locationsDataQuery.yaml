allOf:
  - $ref: DataQuery.yaml
properties:
  query_type:
    description: Type of EDR query
    type: string
    enum: 
      - locations
    examples: 
      - locations
  multiple_locations:
    description: A flag indicating whether the collection supports queries for multiple locations within a single request.
    type: boolean
    enum: 
      - true
      - false
    example: true
